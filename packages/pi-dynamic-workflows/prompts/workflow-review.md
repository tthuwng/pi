---
description: Review a pi-dynamic-workflows workflow spec
argument-hint: "<path-or-workflow-name>"
---
# Review Dynamic Workflow

Review this dynamic workflow spec or workflow name:

$@

Check schema validity, prompt clarity, fanout bounds, output inspection
requirements, dependency on `pi-subagents`, cost/rate-limit risk, and whether
the workflow should be a simpler prompt template or saved chain instead.
